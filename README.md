# claude code history reader

```sh
# pick a `-o` that's in your PATH
go build -o ~/bin/claude-history-reader ./...

# then: go to any folder where you had a claude code session
claude-history-reader

# then: profit
```
